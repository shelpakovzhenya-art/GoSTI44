# Проверка production после выпуска

Дата: 16.09.2026. Релиз: `/var/www/gosti/releases/20260916-1`. Коммит: `b0e89507257b5412e968731e20341a749974f963`.

## Результат

- `gosti`, `nginx` — active; `gosti-php` — running.
- Next `127.0.0.1:3107`, CMS `127.0.0.1:8108`, публичные `/` и `/admin/login` — HTTP 200.
- Два CSS-ресурса текущей сборки — HTTP 200.
- В публичном ответе сохранён `X-Robots-Tag: noindex`; в HTML присутствует meta noindex.
- Candidate-процесс на `3117` после проверки остановлен.
- Production проверен на 320, 390, 768, 1024, 1440 и 2560 px: горизонтального переполнения нет, карточки и кнопки остаются в пределах экрана.
- До переключения созданы `pre-release-20260916-1.sqlite` и `pre-release-20260916-1.env` с правами `600`.

## Откат

Предыдущий frontend-релиз: `/var/www/gosti/releases/20260915-14`. Для отката атомарно вернуть на него `/var/www/gosti/current` и перезапустить `gosti`. Этот выпуск не менял CMS и базу данных.
