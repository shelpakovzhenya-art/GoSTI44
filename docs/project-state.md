# Состояние проекта

Обновлено 18.09.2026.

## Production

- Сайт: https://tangerin.a-test.ru/.
- Активный release: `/var/www/gosti/releases/20260918-18`.
- Frontend: commit `26e9d92`; данные CMS не менялись.
- CMS: `/var/www/gosti/releases/20260918-18/cms`; база: `/var/www/gosti/shared/database.sqlite`; постоянное storage: `/var/www/gosti/releases/20260909-1/cms/storage`.
- Предыдущий рабочий release для отката: `/var/www/gosti/releases/20260918-17`.
- Тестовый домен закрыт от индексации; основной домен заказчик определит позже.

## Последний результат

- На главной восстановлены согласованные высокий hero и плавающая стеклянная шапка поверх реальной фотографии. Из присланного клиентского референса сохранены только три светлые карточки преимуществ под hero.
- Из карточек преимуществ удалены добавленные ранее фотографии и ботанический декор. Вместо них используются три спокойные линейные иллюстрации по темам района, отдельного дома и территории.
- На 1280 px шапка, навигация и три равные карточки помещаются без обрезки. На 390 px кнопки имеют ширину 350 px, карточки — 358 px; горизонтального скролла нет.
- Старых `hero-highlight-photo` и `lemon-branch.webp` в production CSS нет.
- Для восьми карточек блока «Всё необходимое уже здесь» восстановлено отдельное desktop-hover-состояние: подъём на 7 px, тёплая тень и рамка, оранжевая линия, акцент иконки и заголовка. На мобильном эффект не залипает.
- По главной распределены 17 разных ботанических композиций: рейтинги, описание объекта, карточки домов, блок большой компании, преимущества, услуги, веранда, галерея, отзывы, бронирование, FAQ и контакты используют отдельные изображения.
- Повторные `::after`, зеркальные копии и визуальные дубликаты под разными именами отключены. Проверка фактически отрисованных `background-image` на production не выявила повторов.
- В пустых полях у «Перед бронированием» добавлены уникальные ветви айвы и шиповника; у контактов и карты — инжир и мирт. Это четыре новые прозрачные иллюстрации без повторного использования уже существующих веток.
- На desktop новые растения занимают внешние поля секций и не заходят на карточки. На 390 × 844 px они остаются видимыми по краям, не перекрывают текст, карту и нижнюю панель.
- На страницах домов, большой компании и путеводителя растительные акценты также увеличены и стали заметнее, сохраняя внешний край контента.
- Стеклянная шапка стала матовой: непрозрачность светлого слоя и размытие увеличены, навигация получила тёмный контрастный цвет без размытой тени.
- Три иллюстрации преимуществ приведены к композиции клиентского референса: крупная залитая пиктограмма в светло-зелёном круге слева и отдельный детальный рисунок справа. Используются силуэт Костромы с храмами, двухэтажный дом с окнами и озеленением, веранда с растением, мангалом и автомобилем.
- Удалена зависимость видимости секций от `IntersectionObserver`: контент больше не скрывается до анимации при переходе по якорю или изменении ширины окна. Карточки домов, услуги, отзывы, бронирование, FAQ и контакты сразу отрисовываются, поэтому пустые экраны между секциями исчезли.

## Рабочая конфигурация

- На главной и внутренних страницах используются реальные фотографии клиента.
- Работают отдельные страницы домов «Лайм», «Лимон» и «Цитрус», страница большой компании и семь страниц путеводителя по Костроме.
- В публичных текстах используется «дом»; баня находится в разделе «Всё для отдыха», отдельного SPA и пункта меню нет.
- На внутренних страницах используются разные растительные композиции без повторов подряд.
- CMS Filament редактирует тексты, фотографии, SEO-поля и служебные настройки; публичный сайт читает только опубликованные данные CMS.

## Проверка

- Пройдены `verify:content`, TypeScript, ESLint и production build.
- Главная, три страницы домов, страница большой компании, `/kostroma`, CMS API и вход в админку отвечают `200`.
- Production повторно проверен на desktop, на проблемной ширине 647 × 967 px и при viewport 390 × 844 px. Все основные блоки имеют `opacity: 1`, атрибутов ожидания анимации нет, горизонтального скролла нет; выступающие края растений остаются внутри обрезаемых секций.
- `X-Robots-Tag: noindex, nofollow` сохранён.

Предыдущий полный снимок состояния без потерь сохранён в `docs/project-history.md`.

Следующий шаг: после выбора основного домена обновить `SITE_URL`, canonical, sitemap, сертификат и только после отдельной проверки разрешить индексацию.
