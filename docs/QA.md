# Проверки — 9 сентября 2026

## Сайт

- `npm run lint`, `npm run typecheck`, `npm run verify:content`, `npm run build` — пройдены.
- Браузер: 320, 390, 768, 1024, 1440 и 2560 px. Горизонтального переполнения и кнопок за границами экрана не обнаружено.
- Визуально просмотрены первый экран, карточки домов, удобства, SPA, бронирование и футер на мобильном и/или широком экране.
- Проверены мобильное меню, открытие/закрытие описания дома, переключение фото, встроенный модуль бронирования и карта в футере. Внешняя покупка/оплата не выполнялась.
- Hover основной кнопки проверен в браузере: сдвиг вверх на 3 px и появление цветного слоя. Для prefers-reduced-motion предусмотрено отключение движения.

## CMS

`php artisan test`: 9 тестов, 28 проверок. Проверяются разграничение прав, отсутствие черновиков в публичном API, восстановление версии без публикации, очистка HTML, загрузка изображения через форму Filament, запрет редактору управления пользователями, импорт резервной копии и подпись предпросмотра.

В браузере проверено:

1. Изменение заголовка Лайма → сохранение черновика: публичная версия прежняя.
2. Публикация → новый заголовок появился на сайте. Затем исходный текст восстановлен и опубликован.
3. Дополнительная страница: визуальный редактор → HTML → визуальный редактор; форматирование сохранено. Страница опубликована, открыта на сайте и снята с публикации.
4. Защищённый предпросмотр сохранённого черновика открывает сайт, не меняя публичную версию.

Диалог выбора файла браузерной автоматизации не завершился из-за тайм-аута инструмента. Загрузка и сохранение изображения проверены интеграционным тестом самой формы Filament; ручная загрузка в браузере не заявляется как проверенная.

## Границы

Основной домен отложен заказчиком. Локальный сайт и тестовый production `tangerin.a-test.ru` закрыты от индексации. ReservationSteps и карты — внешние сервисы. Их доступность и внутренняя вёрстка зависят от поставщиков. Рейтинги не обновляются автоматически. Перенос пользовательских правок после первого запуска выполняется архивом CMS, а не публичным Git.

## Отдельная установка

Из копии только исходников выполнены npm ci, composer install по lock-файлам, создание новой SQLite-базы, миграции и seed (32 опубликованные записи, 0 пользователей). Сборка Next и все 9 PHP-тестов прошли в новой папке. Данные и зависимости исходной установки не копировались.

Дополнительно: главная 200 с noindex, robots.txt Disallow: /, sitemap.xml 200, снятая с публикации тестовая страница 404. В браузере prefers-reduced-motion отключает анимации кнопок (0s).

Публичный репозиторий проверен: visibility PUBLIC, main совпадает с локальным коммитом. Выполнено отдельное клонирование с GitHub; исходники, фотографии, миграции, lock-файлы и руководства доступны.

## Правка выбора дома — 16.09.2026

По комментарию клиента блок трёх домов перестроен вокруг сценариев поездки. В карточках показаны аудитория, вместимость, площадь, спальни и пять различий планировки. «SPA-зона» в пользовательских текстах заменена на «собственная баня»; навигация и блок бани обновлены тем же термином. Начальные JSON и опубликованные записи CMS синхронизированы.

Проверено в браузере: ширины 320, 390, 768, 1024, 1440 и 2560 px; горизонтального переполнения и обрезанных кнопок нет. Карточки выровнены на desktop, мобильный диалог Лайма помещается в экран, ошибок консоли не обнаружено. Добавлена миграция опубликованных данных CMS; PHPUnit: 19 тестов, 116 проверок.

Финальный визуальный проход выполнен по референсу клиента: заголовок и вводный текст центрированы, карточки получили широкую фотозону, цветовые метки, круглые пиктограммы, компактные характеристики и один контрастный CTA. Блок большой компании собран в общей тёплой панели с фотографией, подписями трёх домов и общей вместимостью. На 768–1000 px используется двухколоночная сетка с третьей карточкой по центру; на мобильном — одна карточка во всю доступную ширину. Повторно пройдены `lint`, `typecheck`, `verify:content` и production-сборка.

## Цвет фотографий — 09.09.2026

Мягкая цветокоррекция при отображении оригиналов: отдельные параметры для территории, интерьеров и бани в CmsImage / editorial.css. Снята зеленоватая окраска тёмных наложений. Оригинальные файлы, детали объектов, фотографии хозяйки и логотипы не изменены. Пробный результат imagegen не используется: он изменял мелкие детали сцены. Проверены desktop и mobile 390 px, применение фильтров и отсутствие горизонтального переполнения; lint и TypeScript прошли.

## Читаемость блока домов — 16.09.2026

По финальному комментарию увеличены вводный текст, заголовки и описания карточек, характеристики, кнопки и подписи блока большой компании. На мобильном основной текст карточек — 16 px, заголовки — 21–23 px, кнопки — 50 px по высоте; desktop-типографика увеличена пропорционально без изменения структуры референса.

Повторно проверены 320, 390, 768, 1024, 1440 и 2560 px: горизонтального переполнения, выхода карточек и кнопок за границы экрана нет. Пройдены `npm run verify:content`, `npm run typecheck`, `npm run lint` и production-сборка с `ALLOW_INDEXING=false`.

## Последние комментарии клиента — 16.09.2026

- Публичное слово «таунхаус» заменено на «дом» в начальных данных и опубликованном контенте CMS. Отдельные страницы трёх домов и большой компании сохранены.
- Отдельный блок и пункт меню SPA удалены. Баня дома «Лайм» перенесена в «Всё для отдыха» как обычная редактируемая услуга с реальной фотографией.
- Добавлены CMS-страницы `/kostroma`, `/kostroma/gde-poest` и `/kostroma/dostoprimechatelnosti`, официальные ссылки и `ItemList` JSON-LD.
- В браузере проверены 320, 390, 768, 1265 и 1440 px: переполнения нет; на 1265 px навигация переключается в компактный режим и не обрезает CTA. Мобильное меню, карточки путеводителя, последовательное появление услуг и нижний CTA проверены после прокрутки.
- Не загружаются битые изображения; runtime exceptions и console errors не обнаружены. Тестовый домен остаётся закрыт от индексации до выбора основного домена.
